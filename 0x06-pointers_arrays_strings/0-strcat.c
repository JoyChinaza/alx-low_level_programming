#include <stdio.h>

int main() {
    // a fuction that catenates two strings
   char destination[] = "*src";
    char source[] = "*dest";
    strcat(destination,source);
    printf("Concatenated String: %s\n", destination);
   
    write("src dest");

    return 0;
}
