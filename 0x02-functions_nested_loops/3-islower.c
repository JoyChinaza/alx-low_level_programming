#include "main.h"

/**
 * main - check the code.
 *Return: Always 0.
 */

int main(void)

{	

char c;
int _islower(int c);

{

	if (c >= 'a' && c <= 'z')

{
		return (1);
}
	else
	{
		return (0);
	}
}
	return (0);
}
